# docker-compose.yml
 
# Reference compose file version 3
version: “3”
services:
# Service name is react-app
  react-app:
# build from Dockerfile
    build: .
# expose 443 as hosting port and container port
    ports:
    - 80:80
